apiVersion: agentpack.dev/v1alpha1
kind: Workspace

packs:
  - path: ./packs/hello

profiles:
  default:
    packs:
      - hello
    targets:
      - codex
      - claude
      - kimi
    scope: project
    installMode: auto
